# List and Tuples

1. What are List ?

List a ordered collection we can multiple items store into this. This is mutable mean
we can modefie this after creat like add, remove, delet using by his functions methods 

    Syntax: my_list = ["item1", "item2"] (Square brackets use hote hain)

2. Common List Methods    
     
    .append(): for add item in last 

    .insert(index, item): anywhere add items using index

    .remove(item): Kisi item ko list se bahar nikalne ke liye

    .pop(): Akhiri item hatane ke liye.

    .sort(): List ko sahi kram (order) mein lagane ke liye.

3. What is a Tuple?

Tuple also a collections. It is Immutable if this creat once time we can't change this and his items  because is immutabe.

    Syntax: my_tuple = ("item1", "item2") (Parentheses use hote hain)

Benefits :- Its very fast and Security is very good     