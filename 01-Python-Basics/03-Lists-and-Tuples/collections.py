fruits = ["Apple", "Banana", "Mango", "Graps", "Gavava"] # I have a list of fruits name 

# we can chack his data type using type function 
print(type(fruits))

# Now we have to print 3rd possition number item remamber to index 

print(fruits[2])

# We can append the item into the list / fruits using the append() function

fruits.append("bumbking")
print(fruits)

# Now Creating the tuple 

tuple = ("haridwar", "Utterperdas", "Jharkhand")
print(tuple)

# we can't appnend the values into the tupple 
# tuple.append(tuple) #here we have show error because tauple is immutable 