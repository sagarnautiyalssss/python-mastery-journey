# Topic: Variables & Memory Management

1. What is a Variable?

Variable like the container or box we stored the the data inside the container mean (Variable name )
In Pythone we dose not need to diclare the data type because it also called (Dynamic Typing)

2. Naming Rules (The Do's and Don'ts)

   . ✅ Valid: my_name, _age, user1 (Snake_case is preferred in Python).
   . ❌ Invalid: 1name, num num2 (blank space), my@name (spacle char)
   . Case Sensitivity: num and Num diffrent in python (Error) 

3. Global vs Local Scope   

In python we have to type of variables 1. Global 2. Local Variable

    1. Global Variable :- Global Variable Diclare outside the block we can use of Global variable anywhere inside outside the function 

    2. Local Variable :- Local Variable declare only inside the block or function we can access use this variable ony inside the block / function

4. Python Magic: Swapping & Multiple Assignment

    Mltiple Assignment in python into single line 
    Example :- a, b, c = 12, 11, 10

    Swapping without creat third variable x, y = y, x

